/*
 * masterexecutor.c
 *
 *  Created on: 2018年8月11日
 *      Author: caojx
 */

#include <stdlib.h>

void masterexecute(int fd) {}

struct masterexecutor *initmasterexecutor(struct master *master) {

	struct masterexecutor * executor = (struct masterexecutor*) malloc(
			sizeof(struct masterexecutor));

	executor->server = master;
	executor->execute = masterexecute;
	return executor;
}
